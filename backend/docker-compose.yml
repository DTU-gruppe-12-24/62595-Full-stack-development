services:
    mysql:
        image: mysql:8.4
        container_name: 62595_mysql
        ports:
          - "3306:3306"
          - "33060:33060"
        volumes:
          - mysql-data:/var/lib/mysql
        environment:
          - MYSQL_ROOT_HOST=%
          - MYSQL_ROOT_PASSWORD=pass
          - MYSQL_DATABASE=fsd
          - MYSQL_USER=user
          - MYSQL_PASSWORD=pass
volumes:
    mysql-data: