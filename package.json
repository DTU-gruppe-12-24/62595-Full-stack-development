{
  "dependencies": {
    "@heroicons/vue": "^2.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.1"
  },
  "scripts": {
    "start:backend": "mvn spring-boot:run",
    "test:e2e": "npm run start:backend & npx wait-on http://localhost:8080 && npx playwright test"
  }
}
